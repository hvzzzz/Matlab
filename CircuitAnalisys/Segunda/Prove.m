f=-5000:5000;
w=2*pi*f;
v=10;
r=20;
L=2;
C=3;
F=1/(2*pi*sqrt(L*C));
w0=2*pi*F;
H=v./sqrt(r^2+(f*L-1./(f*C)).^2);
plot(w,abs(H));
hold on;
    %XL=log(w.^(200/log(2*pi*F)));
    %semilogx(w,XL);
%     VL=H.*XL;
%     hold on;
%     semilogx(w,VL);
%     grid on;
%     axis([1e-10 1e10 -250 2000]);
%     XC=-200*ones(1,length(f));
%     semilogx(w,XC);
%     VC=H*200;
%     semilogx(w,VC);
%     %fprintf('%d\n',l);